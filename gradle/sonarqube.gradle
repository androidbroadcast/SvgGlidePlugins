project(":sample") {
    sonarqube {
        skipProject = true
    }
}

sonarqube {
    properties {
        property "sonar.sourceEncoding", "UTF-8"
        property "sonar.projectName", "SvgGlidePlugins"
        property "sonar.projectDescription", "Plugin for Glide 4 to support SVG"
    }
}
